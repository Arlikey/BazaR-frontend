// import { useFilteredProducts } from "../../../entities/product/queries";
// import type { CatalogFilters } from "../../../features/catalog-filters/model/catalogFilters";

// type Props = {
//   filters: CatalogFilters;
// };

// export default function CategoryProductGrid({ filters }: Props) {
//   const { data, isLoading } = useFilteredProducts(categoryId, filters);
//   console.log(filters);
//   return <>CategoryProductGrid</>;
// }
